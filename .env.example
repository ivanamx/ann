# Vista 3D en modales del lookbook (true | false). Por defecto: false → fotos HD.
VITE_LOOKBOOK_3D=false

# API / PostgreSQL (solo servidor — no exponer en el cliente)
DATABASE_URL=postgresql://ann_atelier:ann_atelier_dev@localhost:5432/ann_atelier
PORT=3001
HOST=0.0.0.0
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=change-this-to-a-long-random-secret

# Admin (para npm run seed:admin)
ADMIN_EMAIL=atelier@annatelier.com
ADMIN_PASSWORD=change-me-now

# Resend (emails de citas)
RESEND_API_KEY=
EMAIL_FROM=Ann Atelier <onboarding@resend.dev>
ATELIER_NOTIFY_EMAIL=atelier@annatelier.com
